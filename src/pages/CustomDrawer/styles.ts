import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconContainer: {
        alignItems: 'center',
        alignContent: 'center',
    },

    icon: {
        width: 114,
        height: 114
    },

    bottomDrawer: {
        marginBottom: 15,
        borderTopWidth: 1,
        borderColor: '#c4c4c4',
    },
});
